<c:forEach var="awardedBadge" items="${badges}">
    <span class="<c:if test="${not awardedBadge.awarded}">faded</c:if>">
        <a href="/badges#${awardedBadge.badge.id}"><techtribesje:badge id="${awardedBadge.badge.id}" /></a>
    </span>
</c:forEach>